import { StringArrayTextDirective } from './string-array-text.directive';

describe('StringArrayTextDirective', () => {
  it('should create an instance', () => {
    const directive = new StringArrayTextDirective();
    expect(directive).toBeTruthy();
  });
});
