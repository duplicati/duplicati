import { EditorHostDirective } from './editor-host.directive';

describe('EditorHostDirective', () => {
  it('should create an instance', () => {
    const directive = new EditorHostDirective();
    expect(directive).toBeTruthy();
  });
});
