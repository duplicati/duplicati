import { ExpandMenuDirective } from './expand-menu.directive';

describe('ExpandMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandMenuDirective();
    expect(directive).toBeTruthy();
  });
});
